#:schema https://raw.githubusercontent.com/YunoHost/apps/master/schemas/tests.v1.schema.json

test_format = 1.0

[default]

    preinstall = """
        sudo yunohost tools update apps
        sudo yunohost domain add baikaldomain.tld
        sudo yunohost app install baikal --force \
            -a "domain=baikaldomain.tld&path=/baikal&password=admin&init_main_permission=all_users"
        """

    # ------------
    # Tests to run
    # ------------

    # -------------------------------
    # Default args to use for install
    # -------------------------------

    # -------------------------------
    # Commits to test upgrade from
    # -------------------------------

    # No upgrade test from 2.6.0: 3.0.0 is a PHP 7.4->8.5 / Silex->Slim rewrite with a
    # changed config format and DB schema. In-place upgrade is supported on a best-effort
    # basis (Doctrine migrations + config regeneration) but is not CI-gated.
